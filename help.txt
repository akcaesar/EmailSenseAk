email-project/
│── backend/             # FastAPI backend
│   │── main.py          # FastAPI app
│   │── requirements.txt # Python dependencies
│── frontend/            # React + Vite frontend
│   │── src/
│   │   │── components/   
│   │   │   │── EmailList.tsx  # Fetches & displays emails
│   │   │── App.tsx       # Main React component
│   │   │── main.tsx      # Entry point
│   │── public/
│   │── package.json      # Frontend dependencies
│   │── tailwind.config.js # Tailwind setup
│   │── vite.config.js    # Vite config
│── README.md





1. Encounterd CORS Problem

Solved by using fetch instead of axios
No the solution turned out to be setting the backend server to be public.


2. Solved how to fetch more emails
Change was to enable ignore errors option in the command 
email_body = msg.get_payload(decode=True).decode(errors="ignore")

3. solved Sumy nltk problem by dowloading

Sumy requires the "punkt_tab" package in addition to "punkt." Paste this line just below nltk.download('punkt'):
nltk.download('punkt_tab')

